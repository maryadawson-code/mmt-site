# Secrets — never commit
.env
.env.local

# Runtime state (published ledger + tokens) — machine-local, never commit
state/published.json
state/tokens.json

# Dependencies / build noise
node_modules/
*.log
.DS_Store
