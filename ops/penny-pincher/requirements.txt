# Penny Pincher — Model Routing Cost Monitor
# Python 3.9+ required (uses pathlib, timezone-aware datetime)
#
# Core dependencies: stdlib only (json, pathlib, logging, signal, datetime)
# No external packages needed for v1.0
#
# Optional (for future Supabase integration):
# supabase==2.4.0
# httpx==0.27.0
